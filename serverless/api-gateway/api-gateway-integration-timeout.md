### APi Gateway Integration timeout

__Integration timeout__ — сколько API Gateway ждёт ответа от backend. Для REST API стандартный предел — 29 секунд. Это относится к Lambda, HTTP, AWS integrations и т.д.
